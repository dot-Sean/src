#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

int
main (
  int argc,
  char * * argv ) {

  size_t len = sizeof (an) / sizeof (int);
    cout <<
     count_if(& (an[0]), & (an[len]), bind2nd(less_equal<int>(), 5)) <<
     endl;
    return (0);
}
