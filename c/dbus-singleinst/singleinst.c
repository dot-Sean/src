//static gboolean single_instance_new_instance (SingleInstanceFactory *factory,
//					      const char **IN_args,
//					      GError **error);

static int
single_instance_create (
  void * a,
  const char * * b,
  void * * c );

#include "single-instance-glue.h"

#include <stdio.h>

static gboolean
single_instance_create (
  void * a,
  const char * * b,
  void * * c ) {
    return (0);
}

int
main (
  int argc,
  char * * argv ) {
    return (0);
}
